<%= form_with(model: placepic) do |form| %>
  <% if placepic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(placepic.errors.count, "error") %> prohibited this placepic from being saved:</h2>

      <ul>
        <% placepic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :pic, style: "display: block" %>
    <%= form.file_field :pic %>
  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
