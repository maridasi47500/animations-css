<%= form_with(model: stuffpic) do |form| %>
  <% if stuffpic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(stuffpic.errors.count, "error") %> prohibited this stuffpic from being saved:</h2>

      <ul>
        <% stuffpic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :pic, style: "display: block" %>
    <%= form.file_field :pic %>
  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
